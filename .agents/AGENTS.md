# Workspace Guidelines & Rules

- **Git Control**: Never run `git commit`, `git push`, or repository creation commands automatically. Always show the terminal commands to the user so they can execute them manually and learn.
- **Comments**: Do not add comments to `.luau` files unless explicitly requested.
- **UI**: Do not dynamically generate UI elements or UI controllers.
- **Main Game ID**: `82614076557684`

